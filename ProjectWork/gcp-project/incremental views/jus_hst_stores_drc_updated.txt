CREATE OR REPLACE VIEW jus_hst_stores_drc
AS
WITH MID AS
(SELECT 
MAX(ST.BATCH_ID) AS MAX_BATCH_ID
FROM ASCENA_STAGING_DEV.JUS_HST_STORES_20190105 ST)
SELECT * FROM ASCENA_STAGING_DEV.JUS_HST_STORES_20190105 ST1, MID
where ST1.BATCH_ID = MID.MAX_BATCH_ID;