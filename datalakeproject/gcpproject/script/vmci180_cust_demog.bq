#!/usr/bin/bash

. ${HOME}/lib/set_env.sh
. ${HOME}/lib/common.sh

#creating the landing table
bq load --replace=true --source_format=CSV --skip_leading_rows=1 --field_delimiter="|" \
        --schema=${schema_path}/edl_landing/vmci180_cust_demog.json \
        edl_landing.vmci180_cust_demog  \
        "gs://${default_bucket}/plus/crm/vmci180_cust_demog_*.txt.gz"
rc_check $? "Load edl_landing"
